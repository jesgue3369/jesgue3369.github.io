// main.js
import { initializeGame } from './gameLogic.js';

document.addEventListener('DOMContentLoaded', () => {
    initializeGame('gameCanvas'); // Inicializa o jogo no canvas com o ID 'gameCanvas'
});
